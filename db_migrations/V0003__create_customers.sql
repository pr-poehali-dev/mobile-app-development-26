CREATE TABLE IF NOT EXISTS customers (id SERIAL PRIMARY KEY, email VARCHAR(200) NOT NULL UNIQUE, name VARCHAR(200), phone VARCHAR(30), address TEXT, password_hash VARCHAR(255), created_at TIMESTAMP DEFAULT NOW());